import React from 'react';

const ShowData = (props) => {
  // console.log(props);
  // const { time } =collect;
  // console.log(img);
  const {id, title} = props.collect;
  
  return (
    <div>
      <h1>only for buy: </h1>
      <h2>Hellleo</h2>
    </div>
  );
};

export default ShowData;